Скрипт, для скачивания фотографий в максимальном разрешении из выбранного альбома
Для авторизации используется OAuth

Вход: main.py -t (access_token) -a (album_id)

access_token - токен полученный через OAuth